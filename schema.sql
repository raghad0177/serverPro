CREATE TABLE cats(
  id SERIAL PRIMARY KEY,
  name varchar(100),
  origin varchar(100),
  temperament varchar(100),
  color varchar(100),
  overview varchar(1000),
  image varchar(1000),
  age varchar(20),
  gender varchar(20)
);